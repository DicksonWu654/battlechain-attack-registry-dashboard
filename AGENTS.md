# Battlechain Dashboard

- Keep all chain lookups read-only against public RPC and registry contracts.
- Preserve a dark, high-contrast layout aligned to Cyfrin/BattleChain documentation styling:
  - Palette should stay navy/dark with `#004DFF` as the primary accent.
  - Keep any future UI animations subtle and purposeful.
- Keep dependencies lightweight and only add packages needed for the dashboard UX.
